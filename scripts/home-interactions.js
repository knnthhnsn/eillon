/* EILLON home-interactions — post-hero homepage wrapper (logic in interactions-core) */
(() => {
  'use strict';
  if (window.EILLON?.__HOME_INTERACTIONS_BOOTED__) return;
  if (window.EILLON) window.EILLON.__HOME_INTERACTIONS_BOOTED__ = true;
})();
