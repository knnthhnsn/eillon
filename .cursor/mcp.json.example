{
  "$schema": "internal-example-only",
  "_comment": "Copy to ~/.cursor/mcp.json (global) or .cursor/mcp.json (project, gitignored). DO NOT commit secrets.",
  "mcpServers": {
    "codebase-memory-mcp": {
      "command": "codebase-memory-mcp",
      "args": []
    }
  },
  "_agent_reach_note": "Agent-Reach is CLI/skill, not MCP — pip install agent-reach && agent-reach install --env=auto. See docs/growth-mcp-setup.md"
}
